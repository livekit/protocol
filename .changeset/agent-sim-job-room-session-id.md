---
"github.com/livekit/protocol": minor
---

Add `room_session_id` to `SimulationRun.Job` so consumers (e.g. dashboards) can link a simulated session to its corresponding room session rather than only to the simulation job id.
