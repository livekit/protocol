---
"github.com/livekit/protocol": patch
"@livekit/protocol": patch
---

redis: `GetRedisClient` accepts a `WithStreamingCredentialsProvider` option for rotated credentials, and `RedisConfig.azure_entra` enables built-in Microsoft Entra ID auth.
