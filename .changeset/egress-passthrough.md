---
"github.com/livekit/protocol": minor
"@livekit/protocol": minor
---

egress v2: add `passthrough` to the `encoding` oneof on `StartEgressRequest` and `ExportReplayRequest`; remove the unused `EgressHandler.UpdateEgress` RPC and the `livekit.UpdateEgressRequest` message
