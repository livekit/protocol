---
"github.com/livekit/protocol": patch
"@livekit/protocol": patch
---

Add `background_noise`, `low_quality_microphone`, and `packet_loss` to `SimulationRun.Create.Request`, so an audio simulation run can impair the simulated user's audio.
