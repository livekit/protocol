---
"github.com/livekit/protocol": patch
"@livekit/protocol": patch
---

agent simulation: structured metric detail on `SimulationRun.JobMetrics` (`unmeasured` reasons, progression flag evidence, per-turn conciseness rationale and failure modes, per-entity outcomes) and `job_ids` on `SimulationRunSummary.Issue`
